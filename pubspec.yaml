name: media_tracker
description: "Personal tracker for movies, series and books."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # State management (theme + future data providers).
  provider: ^6.0.0

  # Persisted local preferences (dark mode, settings).
  shared_preferences: ^2.3.0

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.17.2

  # HTTP client for the TMDB REST API.
  http: ^1.2.0
  file_picker: ^13.1.0

  # Charts for the stats screen (donut + stacked bar).
  fl_chart: ^0.68.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
